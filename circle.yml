dependencies:
    post:
        - make
    cache_directories:
        - node_modules
        - typings
        - dingus/node_modules
        - dingus/typings

machine:
    node:
        version: 4.1.0

test:
    override:
        - make test

general:
    branches:
        ignore:
            - gh-pages
